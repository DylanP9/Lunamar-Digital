# Case study assets

Add approved screenshots / mockups for case studies here, grouped by slug, e.g.

```
case-studies/
  gym-vibe-cafe/
    homepage.png
    menu.png
    location.png
```

Then swap the placeholder visuals in `src/components/MockupCard.tsx` (or render
`next/image` directly in the case-study page) once assets are client-approved.
