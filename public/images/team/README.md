# Team photos

Drop the two co-founder portraits here with these exact filenames:

- `dylan.jpg` — Dylan
- `sasha.jpg` — Sasha

Notes:
- Portrait orientation works best (the cards crop to a 4:5 frame).
- Square or landscape is fine too; faces are kept in frame via the `focus`
  (object-position) value in `src/data/team.ts` — tweak it there if a crop is off.
- Aim for ~800–1200px on the short edge. JPG or WebP.

Until the files are added, each card shows a fallback monogram of the person's
first initial, so the layout still looks intentional.
