# Lunamar Digital — environment variables
#
# The contact form uses FormSubmit, which needs NO API key or env var.
# The site currently requires no environment variables to build or run.
#
# Add any future secrets here (and copy this file to .env.local to set them
# locally). All .env* files except this example are gitignored.
